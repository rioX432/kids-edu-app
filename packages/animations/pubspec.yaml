name: animations
description: Rich animation system for kids educational apps using Rive
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.13.0
  flutter_animate: ^4.5.0
  go_router: ^13.0.0
  design_system:
    path: ../design_system
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
